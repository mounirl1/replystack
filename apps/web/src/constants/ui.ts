/**
 * UI Constants
 * Centralized configuration for UI-related magic values
 */

// =============================================================================
// Timing Constants (milliseconds)
// =============================================================================

/** Duration to show copy success feedback */
export const COPY_FEEDBACK_DURATION = 2000;

/** Duration to show success toast messages */
export const SUCCESS_TOAST_DURATION = 3000;

/** Duration to show info/warning toast messages */
export const INFO_TOAST_DURATION = 5000;

/** Delay for position calculations after DOM updates */
export const POSITION_CALC_DELAY = 100;

/** Delay for tooltip positioning */
export const TOOLTIP_POSITION_DELAY = 150;

// =============================================================================
// Animation Durations
// =============================================================================

/** Standard transition duration */
export const TRANSITION_DURATION = 200;

/** Slow transition duration */
export const TRANSITION_DURATION_SLOW = 300;

/** Fast transition duration */
export const TRANSITION_DURATION_FAST = 150;

// =============================================================================
// Pagination
// =============================================================================

/** Default items per page */
export const DEFAULT_PAGE_SIZE = 10;

/** Items per page for blog posts */
export const BLOG_PAGE_SIZE = 12;

// =============================================================================
// Form Validation
// =============================================================================

/** Minimum password length */
export const MIN_PASSWORD_LENGTH = 8;

/** Maximum name length */
export const MAX_NAME_LENGTH = 255;

/** Debounce delay for search inputs */
export const SEARCH_DEBOUNCE_DELAY = 300;

// =============================================================================
// API
// =============================================================================

/** Default API request timeout */
export const API_TIMEOUT = 30000;

/** Short API request timeout */
export const API_TIMEOUT_SHORT = 10000;

/** Currency detection timeout */
export const CURRENCY_DETECTION_TIMEOUT = 3000;
